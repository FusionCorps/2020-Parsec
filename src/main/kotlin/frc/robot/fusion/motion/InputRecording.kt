package frc.robot.fusion.motion

object InputRecording {

    public var inputs: MutableList<List<Double>> = mutableListOf(listOf(0.0, 0.0))
    var autoFile = ("/home/lvuser/recordedAuto/recording.csv")
    var isRecording = false

    init {

    }
}